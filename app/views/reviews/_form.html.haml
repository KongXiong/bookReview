=simple_form_for([@book, @book.reviews.build]) do |f|
  #rating-form
    %label Rating
  =f.input :comment
  =f.button :submit

:javascript
  $('#rating-form').raty({
    path: '/assets/',
    scoreName: 'review[rating]'
  });

